// Copy this file to config.js and paste your own Unsplash access key below.
// config.js is gitignored, so your key stays out of the repository.
// Get a key free at: https://unsplash.com/developers
//
// Without one the extension still works — the background falls back to a
// gradient, and everything else (clock, quote, focus mode) is unaffected.
//
// Note that an extension is shipped to the people who install it, so a key in
// config.js is readable by anyone who unpacks the built add-on. This keeps it
// out of git and off GitHub; it does not make it a secret.

const PEAKTAB_CONFIG = {
  unsplashAccessKey: 'YOUR_UNSPLASH_ACCESS_KEY_HERE',
};
